import { stringToBuffer } from '../utils/StringToBuffer.js';
export class EpochTemplate {
    epochNumber;
    epochTarget;
    constructor(data) {
        this.epochNumber = BigInt(data.epochNumber);
        this.epochTarget = stringToBuffer(data.epochTarget);
    }
}
