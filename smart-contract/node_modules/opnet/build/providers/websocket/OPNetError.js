import { getErrorMessage } from './types/WebSocketErrorCodes.js';
export class OPNetError extends Error {
    code;
    data;
    constructor(code, message, data) {
        super(message ?? getErrorMessage(code));
        this.name = 'OPNetError';
        this.code = code;
        this.data = data;
    }
}
