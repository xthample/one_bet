export declare class PagingQueryInfo {
    pageSize: number;
    pageNumber: number;
    constructor(pageSize: number, pageNumber: number);
}
export declare class PagingQueryResult<T> {
    pageSize: number;
    pageNumber: number;
    count: number;
    results: T[];
    hasMoreResults: boolean;
    constructor(pageSize: number, pageNumber: number, count: number, hasMoreResults: boolean, results: T[]);
}
