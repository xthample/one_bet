import OPNetTransform from './OPNetTransform.js';
import { Parser } from 'assemblyscript/dist/assemblyscript.js';
export default class OpnetWebTransform extends OPNetTransform {
    private virtualFs;
    readFile(filename: string, baseDir: string): string | null;
    writeFile(filename: string, contents: string | Uint8Array, baseDir: string): void;
    listFiles(dirname: string, baseDir: string): string[] | null;
    afterParse(parser: Parser): Promise<void>;
}
